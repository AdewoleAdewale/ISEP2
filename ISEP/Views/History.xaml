<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="ISEP.Views.History"
             Title="Transaction History"
             BackgroundColor="#0A192F">

    <ScrollView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <StackLayout Padding="20" Spacing="20">
            <Label Text="AGENT TRANSACTION HISTORY" TextColor="White" FontSize="18" FontAttributes="Bold" HorizontalTextAlignment="Center"/>

            <!-- Date Search Container -->
            <yummy:PancakeView BackgroundColor="White" CornerRadius="16" Padding="20" HasShadow="True" Elevation="6">
                <StackLayout Spacing="12">
                    <Label Text="START DATE" TextColor="#0F172A" FontSize="11" FontAttributes="Bold"/>
                    <Frame BackgroundColor="#F8FAFC" BorderColor="#CBD5E1" CornerRadius="8" Padding="10,0" HeightRequest="44" HasShadow="False">
                        <DatePicker x:Name="startDatePicker" Format="D" FontSize="13" TextColor="#0F172A"/>
                    </Frame>

                    <Label Text="END DATE" TextColor="#0F172A" FontSize="11" FontAttributes="Bold"/>
                    <Frame BackgroundColor="#F8FAFC" BorderColor="#CBD5E1" CornerRadius="8" Padding="10,0" HeightRequest="44" HasShadow="False">
                        <DatePicker x:Name="endDatePicker" Format="D" FontSize="13" TextColor="#0F172A"/>
                    </Frame>

                    <Button Text="SEARCH REPORT" BackgroundColor="#1E3A8A" TextColor="White" FontAttributes="Bold" CornerRadius="10" HeightRequest="48" Margin="0,10,0,0" Clicked="Search_Clicked"/>
                </StackLayout>
            </yummy:PancakeView>

            <!-- ListView for History Results -->
            <ListView x:Name="listView" HasUnevenRows="True" ItemsSource="{Binding HD}" SeparatorVisibility="None" HeightRequest="500">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <yummy:PancakeView BackgroundColor="White" CornerRadius="12" Margin="0,6" Padding="15" HasShadow="True" Elevation="3">
                                <Grid RowDefinitions="Auto,Auto,Auto,Auto" ColumnDefinitions="*, Auto" RowSpacing="6">
                                    <Label Grid.Row="0" Grid.Column="0" Text="{Binding payerName}" FontAttributes="Bold" TextColor="#0F172A" FontSize="14"/>
                                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding amount, StringFormat='₦{0:N2}'}" FontAttributes="Bold" TextColor="#059669" FontSize="14"/>

                                    <Label Grid.Row="1" Grid.Column="0" Text="{Binding serviceName}" TextColor="#64748B" FontSize="12"/>
                                    <Label Grid.Row="1" Grid.Column="1" Text="{Binding payRef}" TextColor="#64748B" FontSize="12"/>

                                    <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding payerId, StringFormat='Payer ID: {0}'}" TextColor="#475569" FontSize="11"/>

                                    <Label Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding dateRecorded, StringFormat='{}{0:MMMM d, yyyy h:mm tt}'}" TextColor="#94A3B8" FontSize="11"/>
                                </Grid>
                            </yummy:PancakeView>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </StackLayout>
    </ScrollView>
</ContentPage>