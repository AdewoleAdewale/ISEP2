<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="ISEP.Views.CashoutPage"
             Title="Agent Cashout"
             BackgroundColor="#0A192F">

    <ScrollView HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
        <StackLayout Padding="20" Spacing="20">
            <Label Text="AGENT CASHOUT BALANCE" TextColor="White" FontSize="18" FontAttributes="Bold" HorizontalTextAlignment="Center"/>

            <!-- Balance Display Card -->
            <yummy:PancakeView BackgroundColor="#1E3A8A" CornerRadius="16" Padding="20" HasShadow="True" Elevation="6">
                <StackLayout Spacing="8" HorizontalOptions="Center">
                    <Label Text="AVAILABLE CASHOUT BALANCE" TextColor="#93C5FD" FontSize="12" FontAttributes="Bold" HorizontalTextAlignment="Center"/>
                    <Label x:Name="CashoutBalance" TextColor="White" FontSize="32" FontAttributes="Bold" HorizontalTextAlignment="Center"/>

                    <BoxView HeightRequest="1" BackgroundColor="#3B82F6" Margin="0,5"/>

                    <StackLayout Orientation="Horizontal" Spacing="10" HorizontalOptions="Center">
                        <Label x:Name="Agentname" TextColor="White" FontSize="13"/>
                        <Label Text="•" TextColor="#93C5FD"/>
                        <Label x:Name="AgentSupervisor" TextColor="White" FontSize="13"/>
                    </StackLayout>
                </StackLayout>
            </yummy:PancakeView>

            <!-- Processing Form Card -->
            <yummy:PancakeView BackgroundColor="White" CornerRadius="16" Padding="20" HasShadow="True" Elevation="6">
                <StackLayout Spacing="15">
                    <Label Text="Input your transaction PIN to process cashout. Minimum cashout amount is ₦500." TextColor="#DC2626" FontSize="12" HorizontalTextAlignment="Center"/>

                    <StackLayout Spacing="6">
                        <Label Text="TRANSACTION PIN" TextColor="#0F172A" FontSize="11" FontAttributes="Bold"/>
                        <Frame BackgroundColor="#F8FAFC" BorderColor="#CBD5E1" CornerRadius="10" Padding="12,0" HeightRequest="48" HasShadow="False">
                            <Entry x:Name="Password" IsPassword="True" Keyboard="Numeric" MaxLength="4" Placeholder="4-digit PIN" TextColor="#0F172A" FontSize="14"/>
                        </Frame>
                    </StackLayout>

                    <Button Text="PROCESS CASHOUT" BackgroundColor="#059669" TextColor="White" FontAttributes="Bold" CornerRadius="10" HeightRequest="48" Margin="0,10,0,0" Clicked="ProcessCashout_Clicked"/>
                </StackLayout>
            </yummy:PancakeView>
        </StackLayout>
    </ScrollView>
</ContentPage>